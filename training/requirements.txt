# Runtime for build_mixture.py (dataset download + normalization).
datasets>=2.20.0
huggingface-hub>=0.24.0

# Fine-tuning stack (QLoRA SFT). Install on the GPU host.
# torch is intentionally unpinned here: install the build that matches your CUDA.
# torch>=2.3.0
transformers>=4.44.0
peft>=0.12.0
trl>=0.9.6
bitsandbytes>=0.43.0
accelerate>=0.33.0

# Dev / tests (pure normalizers, no network).
pytest>=8.0.0
